{
  "name": "window-tabs-dev",
  "private": true,
  "engines": { "node": ">=24 <25" },
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "lint": "web-ext lint --warnings-as-errors --ignore-files *.md tests/** scripts/** dist/** .preview/** .github/** package*.json",
    "package": "python scripts/package.py"
  },
  "devDependencies": {
    "web-ext": "10.3.0"
  }
}
