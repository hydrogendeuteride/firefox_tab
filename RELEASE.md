# Release

CI runs tests and builds the ZIP. Push a `v<version>` tag matching `manifest.json` to publish a GitHub release.

Upload the ZIP to [Mozilla](https://addons.mozilla.org/developers/) for signing before distribution.
